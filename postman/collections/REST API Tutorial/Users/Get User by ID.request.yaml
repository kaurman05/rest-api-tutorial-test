$kind: http-request
name: Get User by ID
method: GET
url: '{{baseUrl}}/users/:userId'
order: 3000
pathVariables:
  - key: userId
    value: '{{userId}}'
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
