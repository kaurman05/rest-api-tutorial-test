$kind: collection
description: User management endpoints for CRUD operations. Includes creating users (no auth required), listing users (PAID_USER permission), getting user details, updating user information, and deleting users (ADMIN permission).
order: 2000
