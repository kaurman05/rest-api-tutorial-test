$kind: http-request
name: Delete User
method: DELETE
url: '{{baseUrl}}/users/:userId'
order: 5000
pathVariables:
  - key: userId
    value: '{{userId}}'
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
